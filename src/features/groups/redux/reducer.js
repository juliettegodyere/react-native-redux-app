import { GroupActions } from './actions';

const initialState = {
   
}

const GroupReducer = (state = initialState, action) => {
    return state;
}

const groupReducerConfig = {
    group: GroupReducer
};
export default groupReducerConfig;
