import { SortActions } from './actions';

const initialState = {
    // events:[],
    // loading:true,
    // error:null
}

const SortReducer = (state = initialState, action) => {
   
        return state;
    
}

const SortReducerConfig = {
    sort: SortReducer
};
export default SortReducerConfig;
