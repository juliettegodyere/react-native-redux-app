import { DiscoverActions } from './actions';

const initialState = {
    // events:[],
    // loading:true,
    // error:null
}

const EventsMainReducer = (state = initialState, action) => {
   
        return state;
    
}

const EventsMainReducerConfig = {
    mainEvents: EventsMainReducer
};
export default EventsMainReducerConfig;
