import { DiscoverActions } from './actions';

const initialState = {
    // events:[],
    // loading:true,
    // error:null
}

const DiscoverReducer = (state = initialState, action) => {
   
        return state;
    
}

const DiscoverReducerConfig = {
    discover: DiscoverReducer
};
export default DiscoverReducerConfig;
